# AI Usage Notes - Week 10 - Phuc Khanh

### **1. Ten-Frame Boss Animation and Collision Geometry**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 12, 2026.
* **Prompt used:**

```text
Update the Boss idle and running sprite handling for ten 64x72 animation
frames, and recalculate its crop size, hitbox, and collision box from the new
artwork.

Invert only the Boss sprite horizontally because the authored sheet faces the
opposite direction; do not change movement or behavior.

Adapt the renamed six-frame `boss-run-1.png` sheet while preserving the
64x72 size of each frame and recalculate shifted running geometry.
```

* **Purpose of use:** To make the Boss select and slice the new idle and running sheets correctly and align its combat and navigation geometry with the updated visible sprite.
* **Content generated by AI:** Measured the ten-frame idle and renamed six-frame running sheets, retained fixed `64x72` frame crops, selected the correct frame count per state, centered their combined visible `47x69` bounds, changed the feet collision box to `32x12` with a `28.5`-pixel vertical offset, synchronized the admin-panel Boss defaults, and retained the corrected horizontal sprite orientation.
* **Independent work/validation:** I supplied the updated Boss assets, clarified that `boss-run-1.png` contains six `64x72` frames, and identified that its running poses slightly shift the required hitbox and collision alignment.

### **2. Alternating Boss Idle and Chase States**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 13, 2026.
* **Prompt used:**

```text
Add a Boss idling state that stands still for a random 3-5 seconds, give the
Boss chase state a random 5-7 second duration, and alternate between those two
states in order.
```

* **Purpose of use:** To give the Boss a predictable high-level rhythm with randomized phase lengths instead of continuously chasing and interrupting itself with a separate ranged state.
* **Content generated by AI:** Added a typed `BossIdlingState`, randomized its duration between three and five seconds, randomized `BossChaseState` between five and seven seconds, stopped pathfinding while idle, started and stopped it at chase boundaries, and replaced the former ranged-state interruption with a strict idle-to-chase-to-idle loop while preserving chase movement and contact damage.
* **Independent work/validation:** I specified the alternating phase order and duration ranges so the Boss periodically gives the player breathing room instead of chasing continuously.

### **3. Centralized Entity Collision and Enemy Path Debugging**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 13, 2026.
* **Prompt used:**

```text
Make the admin collision toggle show the hitbox and collision box for every
game entity, and make the enemy-path toggle show pathfinding for every enemy.
```

* **Purpose of use:** To make the admin debug controls consistently cover every live entity collection and every enemy subtype instead of relying on selected entity draw methods.
* **Content generated by AI:** Added one centralized world-debug pass covering team Paladins, level entities, projectiles, and Rover assists; preserved distinct player, enemy, and general-entity colors; made overlapping hitbox/collision outlines simultaneously visible; moved path candidates under the path toggle; and invoked enemy path drawing for every enemy in the level.
* **Independent work/validation:** I observed that the collision toggle omitted non-character entities and that only the Boss called the path renderer, so I required both admin toggles to cover the complete game world.

### **4. Movement-Driven Knight and Boss Animation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 13, 2026.
* **Prompt used:**

```text
For Knights and the Boss, show and animate the running sprite only when they
are actually moving. If they remain in chase state but do not move, show the
idle sprite or idle animation instead.
```

* **Purpose of use:** To prevent blocked or waiting enemies from running in place merely because their AI remains in a chase state.
* **Content generated by AI:** Added a shared movement-animation flag based on actual per-update position displacement and changed Chaser, Ranger, Diver, and Boss sprite selection and frame advancement to use it instead of chase-state membership.
* **Independent work/validation:** I identified the visual mismatch where enemies displayed running animation while pathfinding or collision left their world position unchanged.

### **5. Boss Spell Summoning and Enemy Spawn Lifecycle**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 13, 2026.
* **Prompt used:**

```text
After the Boss idle state, randomly enter either chase or a new spelling
state. Loop the Boss spell animation and summon a random Chaser, Diver, or
Ranger nearby. Apply the shared spawn effect to enemies, hide each enemy until
the third effect frame has played, and keep it disabled through the effect and
for one additional second. Center the effect on its host and align both
bottom edges. Correct the effect so its visible bottom aligns with the enemy's
feet instead of its center.
Make the Boss spelling state last a random four to six seconds and, every
half-second during that state, give it a 50% chance to summon an enemy.
```

* **Purpose of use:** To add a readable Boss summoning phase and ensure newly created enemies enter combat only after a consistent visual introduction.
* **Content generated by AI:** Added a looping six-frame Boss spelling state lasting a random four to six seconds, randomized the post-idle chase/spell branch, and added independent 50% summon rolls every half-second. Implemented deferred nearby Chaser/Range/Diver insertion and a shared nine-frame spawn lifecycle controlling visibility, targeting, damage, collision, movement, and pathfinding before activation. Corrected the sheet crop to its real `64x48` frame size and added explicit sprite-render foot anchors independently of gameplay boxes.
* **Independent work/validation:** I supplied the Boss spell and spawn-effect sheets and specified the reveal frame, one-second activation delay, summon choices, and exact effect-to-host alignment.

### **6. Boss Punch Animation State and Pivot Refactor**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 15, 2026.
* **Prompt used:**

```text
Wire the new Boss punch-ready, punch-play, and detached punch-hand sheets.
Play ready once at state entry, synchronize the body and hand punch frames,
rotate the detached hand toward the player, and randomly choose Punch, Spell,
or Chase after idle. Keep Punch non-damaging while testing the animation and
document each offense probability in code.
Correct the detached punch-hand direction because it currently points away
from the player.
For animation testing, make each Punch state execute five punches in a row.
Mirror the detached hand left and right with the same facing flag as the Boss
while preserving its aim toward the player.
Create a refactor plan for the revised ten-frame ready, four-frame punch-body,
and four-frame detached-hand assets. Play ready once, loop Punch ten times,
and attach hand pivot (8,7) to body point (13,43) while aiming and flipping.
Apply the refactor plan, but preserve the temporary 0/0/100 offense values
because Punch is intentionally forced for animation debugging.
```

* **Purpose of use:** To design and test the complete layered Boss punch presentation before adding combat hit detection or damage.
* **Content generated by AI:** Replaced the obsolete three/six-frame and full-size-hand implementation with the revised asset geometry. `BossPunchState` now owns explicit phase, frame, and completed-cycle state; ready plays once, the four body/hand frames remain synchronized for ten punches, and the compact hand rotates from pivot `(8,7)` while attached to the mirrored body point `(13,43)`. Asset keys now reference `Boss-punch-body.png` and `Hand.png`, and the temporary `0/0/100` offense values remain unchanged.
* **Independent work/validation:** I supplied revised assets with ten ready frames, four punch-body frames, and four compact hand frames; specified hand pivot `(8,7)`, body attachment `(13,43)`, ten repetitions, player tracking, shared facing, animation-only behavior, and preservation of the temporary forced-Punch probability values.

### **7. Boss Fire-Punch Projectile Planning**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 15, 2026.
* **Prompt used:**

```text
Create a plan for the new Fire-Punch Boss projectile. Loop its animation and
fire it at the end of every Punch loop. Make it pass through walls, expire at
the room border, and use the map boundary as a safety limit. Use collision
corners (17,21) and (48,39), align projectile pixel (26,29) with Hand.png
pixel (53,7), initially target the player's current position, then home by at
most 5 degrees per second. Match player-bullet speed and keep speed and turn
rate visible as private BossPunchState variables.
```

* **Purpose of use:** To plan an isolated, animated Boss projectile with exact sprite-pivot alignment, bounded homing, custom collision, and safe room-aware lifetime rules.
* **Content generated by AI:** Designed a specialized `BossFirePunchProjectile`, exact hand-to-projectile transform, rotated collision AABB, per-punch-loop firing hook, opt-in wall-collision bypass, captured room/map boundary checks, and a shortest-angle steering calculation capped by frame time. The plan keeps speed and steering values private in `BossPunchState` and preserves the temporary offense probabilities.
* **Independent work/validation:** I supplied the Fire-Punch asset and specified its frame geometry, collision rectangle, launch and draw pixels, timing, wall behavior, boundary rules, player tracking limit, and requested tuning-variable ownership.
