# AI Usage Notes - Week 12 - Phuc Khanh

### **1. Observer Pattern Implementation Planning (TeamManager to UIManager)**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
implementation plan for this one: Fix #1 (Critical — ~5 pts in Design Patterns): Complete the Observer Pattern
Current issue: UIManager::OnPlayerStatsChanged() in include/UI/UIManager.h is an empty stub ({}) marked "Obsolete". The subject (TeamManager) calls NotifyObservers(), but the observer ignores it and queries TeamManager directly on every frame.
Why it matters: If the grader inspects the code, the Observer pattern may only be graded as "partially implemented" rather than a confirmed pattern.
Fix needed:
Have UIManager store a cached stats structure.
Implement OnPlayerStatsChanged() to update the cached stats when notified.
Let DrawTeamHUD() read from this cached data.
```

* **Purpose of use:** To design a clean, decoupled Observer Pattern implementation where `TeamManager` publishes player and team stat snapshots, and `UIManager` receives and stores them to drive HUD rendering without direct polling.
* **Content generated by AI:** Produced Plan 36 (`plan_36_observer_pattern_hud.md`) detailing the updated `PlayerStatsSnapshot` and `TeamStatsSnapshot` data structures in `IObserver.h`, observer subscription in `GameApplication.cpp`, event dispatches across `TeamManager` and `Paladin`, cached state management in `UIManager`, and automated/manual verification plans.
* **Independent work/validation:** I identified the obsolete stub method in `UIManager.h` from the previous codebase audit and requested a comprehensive implementation plan to turn the Observer Pattern into a fully functional, grading-compliant system.

### **2. Observer Pattern Implementation Execution (Plan 36)**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
let's excute plan 36
```

* **Purpose of use:** To execute Plan 36 by refactoring `IObserver.h`, `UIManager.h`, `UIManager.cpp`, `TeamManager.cpp`, and `GameApplication.cpp` so that `UIManager` observes `TeamManager` via `PlayerStatsSnapshot` and `TeamStatsSnapshot` data events, eliminating the empty stub and caching live stats for the HUD.
* **Content generated by AI:** Implemented the `PlayerStatsSnapshot` and `TeamStatsSnapshot` types in `IObserver.h`; updated `TeamManager::NotifyObservers()` to broadcast snapshots on updates and stat change events; implemented `UIManager::OnPlayerStatsChanged()` and `UIManager::OnTeamStatsChanged()` to store live snapshots; updated `UIManager::DrawTeamHUD()` to render HP, EX, and Quintessence from cached observer snapshots; registered `UIManager` with `teamManager->AddObserver(&uiManager)` in `GameApplication.cpp`; and validated clean CMake build (100%).
### **3. Minimap HUD Upgrade (Custom Room Icons & Outer Active Frame)**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
Context:
We are upgrading the HUD minimap in "Voltron Mission - Galra Cypher". We need to implement custom room-type icons, an outer corner indicator for the player's active room, distinct visited/unvisited node styling, and a translucent rounded background panel with a floor counter.

Task:
Load the new minimap textures, update the minimap drawing logic to use the new node sizes and icon mapping, and render the outer current-room frame with proper layer ordering.
```

* **Purpose of use:** To implement the upgraded minimap rendering pipeline with custom pixel art icons (`minimap_home`, `minimap_event`, `minimap_exit`, `minimap_boss`, `minimap_chest`), 8x8 base node styling with distinct unvisited (`Color{40, 40, 40, 255}`) and visited (`Color{90, 90, 90, 255}`) states, 10x10 outer corner active frame indicator (`minimap_current`), 50% opacity dark gray rounded panel background, and dim white floor counter.
* **Content generated by AI:** Updated `AssetManager.cpp` to load all minimap textures with point filtering; added `RoomType::EVENT` to `RoomNode.h`; rewrote `MinimapRenderer::Draw()` to render the rounded panel background, floor counter, 2px connecting corridors, 8x8 base nodes with centered 6x6 room icons, and the 10x10 active room outer frame; and verified clean CMake compilation (100%).
### **4. Minimap 2x Scaling & High-Contrast Visual Tuning**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
Context:
We are tuning the minimap visuals and scaling in "Voltron Mission - Galra Cypher". The minimap UI elements need to be scaled up 2x for better readability, the background contrast needs to be increased, and the node shades adjusted to be lighter.

Task:
Update the minimap rendering logic to apply a 2x scale to all dimensions (nodes, icons, spacing, outer frame), darken the background panel while boosting opacity to 80%, and brighten both visited and unvisited room node colors.
```

* **Purpose of use:** To double the rendering dimensions of the HUD minimap (16x16 nodes, 32px spacing, 4px corridors, 12x12 room icons via `DrawTexturePro`, 20x20 outer active frame) and enhance readability with an 80% opacity dark panel (`ColorAlpha(Color{ 10, 10, 15, 255 }, 0.8f)`) and brighter node palettes (`Color{ 70, 70, 75, 255 }` for unvisited, `Color{ 140, 140, 150, 255 }` for visited).
* **Content generated by AI:** Updated `MinimapRenderer.cpp` with 2x geometry constants, 80% opacity dark background, brighter room nodes, and `DrawTexturePro` destination rectangles for 12x12 room icons and 20x20 active room corner frame; and verified clean CMake compilation (100%).
* **Independent work/validation:** I specified the 2x scale multipliers, color values for high contrast, and `DrawTexturePro` scaling parameters.

### **5. Small Room Event Layout Restriction & Combat Bypass**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
Context:
We are fixing level generation and room-type assignment bugs in "Voltron Mission - Galra Cypher". Small rooms are incorrectly spawning enemy combat waves. Except for the exit gate room, small rooms are strictly non-combat utility rooms (event or chest rooms).

Task:
Update the dungeon generation, layout loading, and wave management logic to ensure small rooms are strictly assigned as non-combat RoomType::EVENT (using small_01.csv with the event minimap icon) or RoomType::EXIT, completely disabling enemy wave spawns inside them.
```

* **Purpose of use:** To ensure that non-exit small rooms are strictly classified as non-combat `RoomType::EVENT`, loaded with `Small_01.csv` (pots layout), display the `minimap_event` icon on the HUD, and completely bypass enemy wave initialization and door lockdowns.
* **Content generated by AI:** Updated `RoomNode.h` constructor to initialize small rooms as `RoomType::EVENT` with `RoomState::CLEARED`; updated `Tilemap.cpp` template selection to explicitly load `Small_01.csv` and ignore `Small_02.csv` for small non-exit rooms; added strict combat guards in `WaveManager::UpdateDungeonRoom()` to prevent enemy waves from spawning in `RoomType::EVENT`, `RoomType::EXIT`, or `RoomType::SPAWN` rooms; and verified clean CMake compilation (100%).
* **Independent work/validation:** I identified the combat wave spawning bug in small rooms and specified the layout restriction to `Small_01.csv` and `RoomType::EVENT` tagging.

### **6. Minimap 1.5x Scaling & Layer Ordering Optimization**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
Context:
We are adjusting room generation rules and minimap rendering layers in "Voltron Mission - Galra Cypher". The starting room on each floor must be completely empty (no combat/enemies), the minimap element scaling needs to be reduced from 2.0x to 1.5x, and the floor header text must render on the topmost layer to avoid being obscured by map nodes.

Task:
Ensure the spawn room has no enemy spawns, rescale all minimap dimensions by 1.5x, and reorder the minimap drawing pipeline so the floor text is rendered last.
```

* **Purpose of use:** To rescale all HUD minimap dimensions by 1.5x (12x12 nodes, 24px spacing, 3px corridors, 9x9 room icons, 15x15 active room frame) and reorder the 5-layer rendering pipeline so that the floor header text (`Color{200, 200, 200, 255}`) is drawn on top of all nodes and indicators.
* **Content generated by AI:** Updated `MinimapRenderer.cpp` to apply 1.5x geometry constants and moved the floor text draw call to Layer 5 (the final rendering pass); verified `RoomType::SPAWN` safety across `LevelManager.cpp` and `WaveManager.cpp`; and validated clean CMake build (100%).
* **Independent work/validation:** I specified the 1.5x scale factors, layer hierarchy order, and floor text placement.

### **7. Dynamic Minimap Grid Centering & Spawn Room Empty Guarantee**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
Context:
We are fixing layout, centering, and level generation bugs in "Voltron Mission - Galra Cypher". The minimap elements occasionally overflow past the background panel bounds, node/icon/frame alignments are off-center, and the starting spawn/home room incorrectly contains breakable/interactable pots instead of being completely empty.

Task:
Implement bounding box normalization/centering for the minimap grid, fix centering math for all layered node textures, and ensure the spawn/home room contains no pots or interactable props.
```

* **Purpose of use:** To dynamically calculate the grid bounding box (`minGridX..maxGridX`, `minGridY..maxGridY`), calculate total spanned pixels, center the entire layout within the minimap panel, unify `nodeCenter` offset math for base nodes, icons, corridors, and active room frames, and remove all pot/prop spawning inside the starting spawn room.
* **Content generated by AI:** Updated `LevelManager.cpp` to remove pot entity creation in `spawnRoom` and added collision guard to skip procedural prop placement inside spawn bounds; rewrote `MinimapRenderer.cpp` to dynamically compute grid spans, adjust spacing adaptively against available inner panel dimensions, and unify all sub-element coordinates via `nodeCenter`; and validated clean CMake build (100%).
* **Independent work/validation:** I identified the minimap overflow edge cases, formulated the unified `nodeCenter` calculation, and specified that the home/spawn room should contain zero interactable pots.

### **8. Active-Room-Centric Minimap Centering & Scissor Masking**

* **Tool:** Antigravity (Google Deepmind), Gemini 3.7 Flash.
* **Access time:** August 26, 2026.
* **Prompt used:**

```text
Context:
We are refining the camera and rendering rules for the minimap in "Voltron Mission - Galra Cypher". The active/current room node must always be locked to the exact visual center of the minimap panel. Any neighboring nodes that extend past the minimap background boundaries must be cleanly clipped using a scissor mask. Additionally, each room's internal layers (base node, room icon, current room frame) must share a unified per-node center point.

Task:
Update the minimap rendering pipeline to center on the active room's grid coordinates, apply Raylib scissor cropping to the panel bounds, ensure zero pots spawn in the spawn room, and fix the internal sub-element center alignment.
```

* **Purpose of use:** To lock the player's active room to the visual center of the minimap panel, calculate relative node positions `Vector2 nodeCenter = { panelCenter.x + (gx - activeGridX) * spacing, panelCenter.y + (gy - activeGridY) * spacing }`, crop overflowing nodes cleanly using `BeginScissorMode`/`EndScissorMode`, and render the floor header text outside the scissor mask on the topmost layer.
* **Content generated by AI:** Updated `MinimapRenderer.cpp` with active-room-centric offset math, enclosed all map elements inside `BeginScissorMode`/`EndScissorMode`, unified per-node drawing to `nodeCenter` (12x12 node at `nodeCenter - 6`, 9x9 icon at `nodeCenter - 4.5`, 15x15 frame at `nodeCenter - 7.5`), and validated clean CMake build (100%).
* **Independent work/validation:** I designed the active-room camera tracking rule, specified the scissor clipping boundary on the panel rectangle, and verified the empty starting room implementation.