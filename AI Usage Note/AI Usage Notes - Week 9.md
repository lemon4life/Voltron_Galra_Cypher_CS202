# AI Usage Notes - Week 9 - Phuc Khanh

### **1. Dynamic Dungeon Sizing and Depth Rendering**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
hey gemini, claude fix the previous bug for me already. Now help me to:
- in the bottom walls, the fronts walls should also be shown, not just the top.
- the room is currently quite small, make the map grid bigger (e.g 20x20 tiles for normal, interaction (chest, exit, ...) room and 32x32 tiles for enemies and boss room). 
- the path is currently too small for the character to pass through, make the path's width 5 floors tiles.
```

* **Purpose of use:** To increase combat arena sizes, widen corridors for easier navigation, and fix bottom-facing walls drawing without 3D depth into the void.
* **Content generated by AI:** Updated `Constants.h` to define `MAX_ROOM_TILE_SIZE` (32), `NORMAL_ROOM_TILE_SIZE` (20), and `CORRIDOR_WIDTH` (5). Refactored `Tilemap.cpp::BakeLevel` to dynamically size rooms, centering normal rooms (20x20) inside a 32x32 cell offset. Updated path carving in `BakeLevel` to widen corridors to 5 tiles. Updated `TilemapRenderer::DrawRoom` to check if a tile below a wall is a floor OR a void (`tileID == 2`), so bottom-facing walls properly draw their front faces into the void.
* **Independent work/validation:** Adjusted `LevelManager.cpp` spawn calculations and room trigger bounds mathematically to align with the new dynamic room centering and map offsets.

### **2. Gate Animation and Corridor Continuity Fix**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
the transfer gate isn't rendered properly and animated (it has 8 frames), also, it seems to small. The void in the map should be dark not grey. In the map, some path is discontinue (half void and half path)?.
```

* **Purpose of use:** To fix the static, undersized exit gate rendering, change the void background to black, and resolve disconnected paths caused by centering the 20x20 rooms inside 32x32 cells.
* **Content generated by AI:** Updated `LevelManager::DrawLevel()` to draw the exit gate at 4x tile scale and calculate the current animation frame using `(int)(GetTime() * 10) % 8` mapped to `gateFrameSrc`. Changed `ClearBackground(DARKGRAY)` to `ClearBackground(BLACK)` in `main.cpp` for gameplay rendering so the void is completely black.
* **Independent work/validation:** Fixed `Tilemap.cpp::BakeLevel` corridor carving by adding directional passes for `node->west` and `node->north`. Corridors now fully bridge the cell offset space for 20x20 rooms, creating continuous paths with adjacent cells.

### **3. Minimap Overhaul, Camera Zoom, and Door Barricades**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
make the camera zoom further, there are bugs: the character escaped the room even when the room isn't cleared. 

new features:
- the minimap should show: are there adjecent rooms nearby the current room the character is in, and which room are there like in this picture.
```

* **Purpose of use:** To zoom out the camera for better visibility in 32x32 rooms, fix a lockdown bug allowing the player to bypass the barricade, and overhaul the minimap to show adjacent unvisited rooms with icons (acting as a roguelike radar).
* **Content generated by AI:** Multiplied `camera.zoom` by `0.75f` in `CameraManager::UpdateCamera` to widen the global view. Updated `Tilemap.cpp::BakeLevel` door anchor generation to spawn `layer1_objects = 20` across the entire `Constants::CORRIDOR_WIDTH`, creating a solid 5-tile barricade during lockdowns. Completely rewrote `MinimapRenderer::Draw` to add an `isRevealed` helper that checks if a room is discovered or shares an edge with a discovered room.
* **Independent work/validation:** Updated the minimap rendering to draw undiscovered but revealed rooms as dark gray, draw their connecting corridors, and render room type icons (including a new yellow `!` for `RoomType::BATTLE`) to preview adjacent challenges.


### Prompt:
Update the Constants.h (or main settings file) in this C++ Raylib project. Change the base TILE_SIZE from 32 to 16. Introduce a new constant constexpr float GLOBAL_SCALE = 3.0f; and a derived constant constexpr float RENDER_TILE_SIZE = TILE_SIZE * GLOBAL_SCALE;. Ensure all map grid calculations now use RENDER_TILE_SIZE instead of a hardcoded 32.

Update the texture loading logic to load the new map assets. Define Texture2D variables for the tilesets and load them using these exact file paths:
assets/tileset/Galra_Floors.png
assets/tileset/Galra_Walls.png
assets/Objects/box.png
assets/Objects/Transfer_gate.png

Also, set up standard Raylib Rectangle source structures to slice these 16x16 tiles:
floorBaseSrc: x=0, y=0, w=16, h=16
wallTopSrc: x=0, y=0, w=16, h=16
wallFrontFaceSrc: x=0, y=16, w=16, h=16

### Purpose:
To transition the map grid and textures to a higher resolution standard where raw tiles are 16x16 and globally scaled by 3.0f to 48x48. This ensures that the assets (floors, walls, destructible boxes, and transfer gates) are loaded from new files and drawn using proper slicing structures, making the game visually cohesive and enabling higher asset fidelity.

### Content Generated:
- Updated `Constants.h` to define `TILE_SIZE` (16.0f), `GLOBAL_SCALE` (3.0f), and `RENDER_TILE_SIZE`.
- Refactored map grid calculations in `LevelManager.cpp` and `Tilemap.cpp` to use `Constants::RENDER_TILE_SIZE` instead of hardcoded 32, and updated the `DestructibleBox` size to align with the new standard.
- Loaded the 4 new textures (`Galra_Floors.png`, `Galra_Walls.png`, `box.png`, `Transfer_gate.png`) via `LevelManager` and corrected the texture path for the box in `AssetManager`.
- Defined standard Raylib Rectangle source structures in `TilemapRenderer::DrawRoom` to correctly slice the 16x16 tile assets: `floorBaseSrc`, `wallTopSrc`, and `wallFrontFaceSrc`, replacing simple rectangles with `DrawTexturePro` logic.


### Prompt:
Update the texture loading logic to load the following assets from these exact paths using Raylib's LoadTexture():
assets/tileset/Galra_Floors.png
assets/tileset/Galra_Walls.png
assets/Objects/box.png
assets/Objects/Transfer_gate.png

Define the following Rectangle source structures and arrays to slice these 16x16 tiles:
Rectangle wallTopSrc[2] with values {0, 0, 16, 16} and {16, 0, 16, 16}.
Rectangle wallFrontFaceSrc[2] with values {0, 16, 16, 16} and {16, 16, 16, 16}.
Rectangle floorSrc[6] mapped dynamically in a loop or statically from x=0 to x=80, all with y=0, w=16, h=16.
Rectangle boxTopSrc = {0, 0, 16, 16};
Rectangle boxBottomSrc = {0, 16, 16, 16};
Rectangle gateFrameSrc = {0, 0, frameWidth, frameHeight}; (Calculate frameWidth = texture.width / 8.0f).
Apply SetTextureFilter(texture, TEXTURE_FILTER_POINT); to all loaded textures to keep the pixel art crisp."

Write a C++ Raylib render function for a 2D map grid (map[x][y]). Iterate through the grid and calculate the world position using x * RENDER_TILE_SIZE and y * RENDER_TILE_SIZE.

Create a simple pseudo-random hash based on the (x, y) coordinates to pick tile variants deterministically:
If the cell is FLOOR, calculate variant = hash(x, y) % 6. Render floorSrc[variant] from Galra_Floors.png.
If the cell is WALL, calculate variant = hash(x, y) % 2. Draw wallTopSrc[variant] from Galra_Walls.png.

Add depth logic: After drawing a WALL cell, check if the cell directly below it map[x][y+1] is a FLOOR. If it is, calculate a new destination rectangle shifted down by one tile ((y+1) * RENDER_TILE_SIZE) and draw wallFrontFaceSrc[variant] over that floor tile to create a 3D wall face.

### Purpose:
To enhance the visual fidelity of the map by introducing deterministic pseudo-random variants for floors and walls using a hashing function, and by establishing an initial 2.5D depth illusion by rendering a front-facing wall texture over floor tiles immediately below wall cells. It also refines how boxes and exit gates are rendered by specifying exact sliced source rectangles.

### Content Generated:
- Defined specific `Rectangle` slice arrays (`wallTopSrc`, `wallFrontFaceSrc`, `floorSrc`) inside `TilemapRenderer::DrawRoom` to support variants.
- Implemented a pseudo-random hash lambda function inside `DrawRoom` based on `(x, y)` grid coordinates to pick specific visual variants deterministically.
- Added depth rendering logic that checks if the cell directly below a wall tile is a floor, and if so, draws the `wallFrontFaceSrc` texture down one tile.
- Applied the `boxTopSrc` and `boxBottomSrc` rendering split inside `DestructibleBox::Draw()`.
- Updated the exit gate drawing in `LevelManager.cpp` to correctly slice a frame width (`texture.width / 8.0f`) using `gateFrameSrc`.


### Prompt:
are you sure you update the tileset to 16x16? compared to the character (whose sprite is 32x32) the tileset is extremely huge, and the camera is too zoomed in, fix it, btw, the floors and wall renders should be somekind of randomly choose, not fix the same one again and again

### Purpose:
To resolve scaling mismatches where the tileset (16x16 pixels) appeared disproportionately huge compared to the character (32x32 pixels), fix the excessive camera zoom, and apply a better pseudo-random hash algorithm to ensure the floor and wall variants are distributed naturally without repeating blocks.

### Content Generated:
- Disconnected `GLOBAL_SCALE` from `Constants::RENDER_TILE_SIZE` (changing it from `TILE_SIZE * GLOBAL_SCALE` back to just `TILE_SIZE`, so `RENDER_TILE_SIZE` is now exactly 16.0f in world coordinates). This maps 1 tile directly to a quarter of the character's 32x32 bounding frame, aligning the pixel resolution perfectly and ending the "extremely huge" effect.
- Reduced `Constants::GLOBAL_SCALE` from 3.0f to 2.0f, which naturally reduced the zoom level applied by the `CameraManager`, widening the view significantly so more of the room is visible without losing the pixel-art aesthetic.
- Updated `LevelManager.cpp`'s call to `GenerateWallColliders` to use a fixed scale of `1.0f` so that collision hitboxes accurately reflect the new, smaller visual tiles instead of producing ghost collisions twice their size.
- Improved the pseudo-random hashing algorithm in `Tilemap.cpp`'s `DrawRoom` method to use prime multiplication and bit shifting, solving the repeating artifacts issue and properly distributing tile variants dynamically.


### Prompt:
The are bugs in the story mode:
- there are no walls bounding the map
- the floors isn't randomized
- the character got kicked out of the map and can't move. 

### Purpose:
To fix legacy map (story mode) rendering and collision. The previous switch to 16x16 tiles broke the CSV-based legacy map because the `tileID` indices were no longer mapped correctly to the new `Galra_Floors.png` and `Galra_Walls.png` tilesets, causing walls to draw as floors and effectively disabling collision bounds.

### Content Generated:
- Rewrote the `LevelManager::DrawLevel()` logic for the legacy map to correctly interpret `tileID == 0` as a wall and route it to `Galra_Walls.png` with depth faces, while routing all other values to `Galra_Floors.png`.
- Applied the same bitwise pseudo-random hashing function from the dungeon generator to the legacy map's tiles, ensuring randomized floors and walls in story mode too.
- Fixed `LevelManager::IsSolidCollision()` for the legacy map. It previously checked `tileID == 1` for walls, which allowed the player to walk through the actual boundary walls (`tileID == 0`). This caused the player to step out of bounds and become permanently stuck in the hardcoded solid void region. The collision check now correctly identifies `0` as the solid boundary wall.


### Prompt:
the character still get stuck outside the map, also, the wall should only bound around the maps, outside the walls, there should be a blank dark area (the void).

### Purpose:
To resolve issues where walls were rendering infinitely out of bounds, and an oversight in the collision logic where the right side of the map bounds wasn't properly checked as solid, allowing the character to clip out and get stuck.

### Content Generated:
- Removed the `else if (layer == 1) { isWall = true; }` condition in `LevelManager::DrawLevel()`. Now, out of bounds areas are skipped during rendering entirely, leaving them as a blank dark void instead of a repeating wall texture.
- Fixed a boundary checking flaw in `LevelManager::IsSolidCollision()` where it checked if `c >= 0` but missed checking `c < gridCols`. This meant any collision check outside the right border of the map did not evaluate as an "Out of bounds solid void" and instead fell through as non-solid. This allowed the player to dash through the right boundary wall and get stuck outside the map where clamping locked them down. The check is now correctly bounded by `c < gridCols`.


### Prompt:
now there is no walls, and the void outside is suddenly green? For now, you should ignore all the .csv files and things. Remember to render WALLS the top and the fronts of it. 
The character is still got stuck outside the map again.

### Purpose:
To bypass the legacy CSV map entirely in favor of a procedurally generated Hub map, fix the green background, ensure wall depth is drawn for legacy maps, and ensure the player can progress from the Hub.

### Content Generated:
- Hardcoded `LevelManager::LoadLevel` to procedurally generate a 20x20 tile room when loading a legacy map (ignoring CSV data). The room has a solid border of wall tiles.
- In `main.cpp`, changed the `ClearBackground` color for `GameState::HUB` from `DARKGREEN` to `BLACK` to fix the void rendering as green.
- Updated `LevelManager::DrawLevel()` so that boundary walls in the legacy map always render their 3D front faces unless directly obscured by another wall tile below them.
- Spawned an `NPC` entity in the center of the Hub room instead of a raw transfer gate, allowing the player to actually trigger the `GameState::HUB` dialogue and transition properly instead of wandering indefinitely in a large map without any exit.


### Prompt:
the character still got stuck like this: 
[User provided screenshot showing Lance half off-screen on the right edge, softlocked inside the boundary]

### Purpose:
To resolve a fatal boundary collision logic flaw that was allowing the player's position to become permanently softlocked inside the physical map boundary walls.

### Content Generated:
- Rewrote the X and Y movement collision resolution in `PlayerState.cpp` (for `PlayerRunState`), `PlayerDashState.cpp`, `PlayerAttackState.cpp`, and `Paladin.cpp` (for knockback).
- The original logic subtracted `moveDelta` from the post-clamped `currentPos`. This caused the player to become permanently embedded in the wall collision volume if they walked or dashed off the map edge, as the post-clamped revert logic pushed them backward into the wall block rather than safely into the room.
- Updated the resolution logic to cache a `Vector2 prevPos` before movement and directly assign `currentPos = prevPos` upon any solid collision. This guarantees the player is always returned to an explicitly valid coordinate and mathematically eliminates the possibility of being softlocked inside collision bounds.


### Prompt:
the character still got stuck like this: 
double-check again, they are still outside the map (the void), check the map boundary and the character's begining location, always make sure they are within the boundary! 

Also, is it possible the make the character has the fat pixel technique like their weapons. Because when the character move around in some postion, their pixel got distorted and weird out. 

### Purpose:
Fix visual sub-pixel rendering distortion for the character sprite ("fat pixel" technique) and resolve the visual confusion where the character appeared to be stuck outside the map due to thin boundary walls and camera bounds exposing the black void.

### Content Generated:
- `src/Entities/Player/Paladin.cpp`: Added integer snapping (`std::floor`) to the character's render destination rectangle (`destRec`) in `Draw()` to ensure crisp "fat pixel" rendering without sub-pixel blurring when moving.
- `src/Core/Manager/LevelManager.cpp`: Expanded the Hub procedural map from 20x20 to 30x30 and increased the border wall thickness from 1 tile to 5 tiles. This ensures the player is fully bounded far away from the camera limits, preventing the void from being visible on-screen.
- `src/Core/Manager/LevelManager.cpp`: Modified the legacy wall rendering depth logic to *always* draw the front faces of the border walls, resolving the issue where the outer boundary just looked like floor tiles.
- `src/main.cpp`: Updated all instances of the player's initial spawn coordinates to `720.0f, 720.0f` to perfectly center them in the new 30x30 map grid (15 * 48 = 720).



### Prompt:
Now the character is missing (wtf), the void is also disappear and replaced by multiple walls? the bugs are even worsed after you fixed and no, the map isn't largen at all. 

### Purpose:
Revert the 30x30 Hub map and deep wall changes that caused unintended side effects (overlapping wall front faces, off-screen spawn locations making characters missing). Implement the proper fixes for sub-pixel distortion and boundary overlapping without breaking the legacy map rendering.

### Content Generated:
- `src/Core/Manager/LevelManager.cpp`: Reverted the procedural map generation back to 20x20 with 1-tile thick walls to restore the correct Hub layout. Reverted the `tileBelowIsFloor` rendering logic to prevent the "multiple walls" overlap glitch.
- `src/main.cpp`: Reverted the paladin spawn coordinates from `(720, 720)` back to `(256, 256)` and `(400, 300)`, fixing the issue where characters spawned off-camera or were considered missing.
- `src/Core/Manager/AssetManager.cpp`: Applied `TEXTURE_FILTER_POINT` directly to all loaded Paladin sprite textures. This is the correct, native Raylib way to achieve the "fat pixel" crispness without mathematically snapping the `destRec` coordinates, ensuring smooth physics and no sub-pixel distortion.
- `src/Entities/Player/Paladin.cpp`: Expanded the player's collision box width from `16` to `32` to exactly match the visual sprite width. This stops the player from physically walking halfway into the 48px boundary walls, preventing the visual illusion of being "stuck inside/outside the wall".


---

# AI Usage Notes - Week 9 - Phuc Khanh (Merged Local Entries)

### **1. Main and Selection-HUD Merge Plan**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 3, 2026.
* **Prompt used:**

```text
Create a Markdown plan for merging the main and Selection-HUD branches.
```

* **Purpose of use:** To prepare a safe integration sequence and define how overlapping level, wave, navigation, Hub-selection, minimap, audio, and main-loop changes should be combined.
* **Content generated by AI:** Inspected the branch merge base, unique commits, changed files, predicted conflicts, map assets, and remaining legacy-map references; then created Plan 13 with a temporary integration-branch workflow, file-specific conflict resolutions, asset reconciliation, implementation order, validation checks, and completion criteria.
* **Independent work/validation:** I requested a documented plan before any merge was performed. The intended result keeps both branches' current features instead of replacing either branch wholesale.

### **2. Main and Selection-HUD Merge Implementation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 3, 2026.
* **Prompt used:**

```text
Apply Plan 13 to merge main and Selection-HUD into the project.
```

* **Purpose of use:** To combine the two diverged branches while preserving the layered Hub and Paladin selection alongside procedural rooms, minimap, room waves, audio, and the safer enemy-navigation contract.
* **Content generated by AI:** Created safety and integration branches; merged `main` into `Selection-HUD`; resolved `LevelManager`, `WaveManager`, and `main.cpp`; introduced explicit layered/procedural level modes; retained optional no-fallback pathfinding; migrated remaining Hub and demo references away from legacy text maps; updated procedural enemy spawning to the `MapObjectId` factory; combined Hub selection with modular missions and minimap rendering; and removed duplicated sound loading inherited from `main`.
* **Independent work/validation:** I approved Plan 13 for implementation and required both branches' current features to remain available in the combined project.

### **3. Minimap and Team HUD Separation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 3, 2026.
* **Prompt used:**

```text
Move the minimap to the rightmost corner so it does not overlay the Team HUD.
```

* **Purpose of use:** To keep the procedural-dungeon minimap visible without covering the three-Paladin status interface.
* **Content generated by AI:** Reduced the minimap panel from 150 to 140 logical pixels and anchored it with a four-pixel right inset, placing its left border beyond the Team HUD's maximum right edge while retaining its top-right position.
* **Independent work/validation:** I requested that the minimap remain at the far-right corner and not overlap the Team HUD.

### **4. Enemy Navigation Footprint and Wall-Adjacent A* Plan**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
Create a complete plan to fix dumb enemy A* behavior when the player stands
too close to a wall by separating enemy collision boxes from their hitboxes,
similar to the player.
```

* **Purpose of use:** To identify why wall-adjacent player positions break enemy navigation and define a reliable correction that keeps movement collision separate from combat collision.
* **Content generated by AI:** Inspected the current enemy and Paladin rectangles, wall movement, spawn validation, A* occupancy and fallback behavior; then created Plan 14 covering configurable per-enemy navigation footprints, separate contact regions, reachable approach goals around the player, multi-goal A*, complete-edge checks, real-position anchors, no direct fallback, bounded caching, debug overlays, and validation scenarios.
* **Independent work/validation:** I proposed using a player-like enemy movement box distinct from the hitbox and identified the reproducible case where enemy pathfinding behaves poorly when the player stands close to a wall. I will review and calibrate each enemy footprint against the intended in-game feel before implementation.

### **5. Enemy Navigation Footprint and Wall-Adjacent A* Implementation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
Apply Plan 14 to fix enemy A* navigation when the player stands close to a
wall.
```

* **Purpose of use:** To make enemy movement and A* use configurable feet-sized navigation footprints while keeping combat collision independent and preventing direct movement through walls after a failed search.
* **Content generated by AI:** Added per-enemy navigation profiles, separate damage and contact rectangles, optional path targets and explicit search statuses, wall-adjacent approach candidates, bounded multi-goal A*, complete sampled edge validation, real-position grid anchoring, per-search collision caching, navigation-based spawn validation, and collision/path debug drawing.
* **Independent work/validation:** I chose the requested player-like feet-box approach and retained different footprint sizes for Chaser, Ranger, Diver, and Boss so they can be calibrated independently. I also required unreachable searches to produce no player-facing movement direction rather than preserving the old direct fallback.

### **6. Enemy Pathfinding Branch Merge**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
Merge Enemy-Path-Finding into main and document the merge process in a
Markdown file.
```

* **Purpose of use:** To integrate the newer enemy navigation implementation into the main project while preserving the Selection-HUD integration and current Ranger behavior.
* **Content generated by AI:** Compared branch ancestry and file changes, merged `Enemy-Path-Finding` into `main`, reconciled the enemy navigation interfaces and debug systems, retained the newer direct-fire Ranger behavior, combined both Week 9 declarations, documented conflict decisions, and validated the integrated source tree.
* **Independent work/validation:** I required the completed merge to live on `main` and requested a written record of the merge process.

### **7. Shared 30-Pixel Enemy Path Goals**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
Refactor enemy pathfinding so every enemy targets the same points around the
player. Do not pre-check point collision; mark points green or red only from
line of sight, remove the old unnecessary goal functions, and place the shared
target circle at a 30-pixel radius. For each candidate, test line of sight to
the nearest point on the player's hitbox instead of the player's center.
```

* **Purpose of use:** To simplify goal selection into one predictable rule shared by every enemy and make candidate validity reflect whether each point can see the nearest visible part of the player's hitbox rather than requiring visibility to its center.
* **Content generated by AI:** Replaced per-enemy approach distances and goal validators with eight shared radius-30 candidates; calculated the closest clamped point on `Paladin::GetBoundingBox()` for every candidate LOS query; deferred footprint occupancy to A* goal-anchor construction; and removed the obsolete path-goal types, overrides, constants, and unused target-position accessors.
* **Independent work/validation:** I specified that all enemy types must use the same surrounding points and that collision must not determine their debug color before pathfinding attempts to reach them.

### **8. Player and Enemy Collision Debug Outlines**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
Show the player and enemy hitboxes and collision boxes with a different color
for each one, using small, simple outline widths. Keep the surrounding goal
points but remove the LOS probe debug entirely.
```

* **Purpose of use:** To make movement and damage geometry easy to distinguish during gameplay without large or visually heavy debug shapes.
* **Content generated by AI:** Added one shared entity-collision debug toggle, thin 0.75-pixel outlines, blue and green player boxes, red and orange enemy boxes, and centralized enemy debug drawing; retained the green/red surrounding goal points without storing or drawing individual LOS probes.
* **Independent work/validation:** I requested four distinct colors and small line sizes so the overlay remains readable without covering the game artwork.

### **9. Developer Admin Panel**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 4, 2026.
* **Prompt used:**

```text
Create an isolated adminGUI panel that can be enabled from Constants, toggle
debug flags, place any enemy type with the mouse, edit all selected-enemy data
through a scrollable interface, and enable player immunity with minimal changes
outside the debug module.

Increase the admin-panel text size and replace live-enemy number editing with
sliders whose values configure the next spawned enemy instead.
```

* **Purpose of use:** To provide developers with a readable in-game control panel for inspecting geometry, preparing custom enemy spawn presets, placing test enemies, and testing combat without player death.
* **Content generated by AI:** Added a self-contained `UI/adminGUI` panel with F1 visibility, larger text and controls, mutable debug and immunity toggles, Chaser/Ranger/Diver/Boss placement through the existing entity factory, and scrollable sliders that maintain separate next-spawn combat, hitbox, and navigation presets for every enemy type without changing existing enemies.
* **Independent work/validation:** I required the panel to remain a debug-only feature with one constants flag, minimal interference with normal gameplay files, and preset values that affect only newly spawned enemies.


### **10. 2.5D Depth Sorting and Procedural Props Plan**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 6, 2026.
* **Prompt used:**

```text
Implement a 2.5D depth-sorting rendering pipeline for tall entities and walls by splitting 16x32 sprites into top and bottom halves. Then, implement a procedural prop spawner to populate battle rooms.

New Assets in `assets/Objects/`:
- `box.png` (16x32): Destructible box.
- `object_1_8.png` (128x32): Indestructible animated decoration (8 frames of 16x32).
- `object_2.png` (16x32): Indestructible static decoration (1 frame).

Requirements:
1. Sprite Splitting & Layered Rendering Logic...
2. Bounding Boxes & Physics (Feet-Based Collision)...
3. Animated Objects...
4. Procedural Room Mock-up (Prop Placement)...
```

* **Purpose of use:** To establish a 2.5D visual hierarchy where characters correctly overlap tall props/walls depending on their Y-coordinate, and to add dynamically spawned props (boxes, animated tubes) inside battle rooms for environmental variety and cover.
* **Content generated by AI:** Analyzed existing drawing logic in `main.cpp` and `LevelManager.cpp`. Designed `plan_33_depth_sorting.md` detailing a unified two-pass rendering pipeline: `Pass 1` for floor/bases and `Pass 2` containing a sorted list of characters and prop/wall "overhangs" (top 16x16) based on their feet Y-coordinates. Outlined a new `Prop` entity system with bottom-only AABB collisions, and modified `RoomTemplate` to randomly spawn 2-6 props per battle room.
* **Independent work/validation:** Clarified that drawing top halves *before* the player when the player is in front, and *after* the player when the player is behind, can be natively solved using standard list-based Y-sorting.

### **11. Boss Sprite Drawing and Animation Plan**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 6, 2026.
* **Prompt used:**

```text
From the added Boss idle and running sprite sheets, create a plan for updating
the Boss drawing.

Inspect the Boss images carefully, resize the Boss hitbox relative to the real
visible sprite instead of keeping the old size, and apply the changes.
```

* **Purpose of use:** To replace the Boss's temporary rectangle with the new animated artwork and derive its combat and navigation geometry from the real visible sprite size.
* **Content generated by AI:** Measured the alpha bounds across both four-frame `512x128` sheets, implemented point-filtered idle/running animation and directional feet-aligned drawing, resized the contact hitbox to `96x124`, resized the navigation footprint to `56x18` with a `{0, 53}` offset, updated Boss admin-spawn defaults, repositioned the health bar, and retained a missing-texture fallback.
* **Independent work/validation:** I supplied the Boss sheets and identified that keeping the temporary `64x72` hitbox would not match the new artwork. I required the implemented hitbox to reflect the visible Boss while keeping the feet footprint narrow enough for the current five-tile corridors.

### **12. Enemy Push Resistance and Boss Facing Stability**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 6, 2026.
* **Prompt used:**

```text
Create a resistance value for each enemy so player collision cannot push
resistant enemies, apply the same anti-knockback behavior to player shots
especially for the Boss, and stop the Boss from repeatedly reversing left and
right. Add debug drawing for the Boss hitbox. Remove the facing dead zone and
reverse the Boss sprite orientation because the source artwork faces the
opposite direction.
```

* **Purpose of use:** To give enemy types distinct physical weight, prevent collision separation and weapon impacts from moving resistant enemies excessively, and stabilize Boss rendering while the player overlaps its center.
* **Content generated by AI:** Added a clamped per-enemy resistance value shared by hit knockback and collision/parry separation; configured Ranger, Chaser, Diver, and Boss resistance as `0.10`, `0.25`, `0.50`, and `1.00`; added an admin spawn slider; connected the Boss draw path to the existing red hitbox and orange navigation-box debug overlay; and inverted the Boss sprite flip relative to its target without a facing dead zone.
* **Independent work/validation:** I observed that player contact and shots could move enemies too easily, especially the Boss, and that close overlap made the Boss alternate facing direction. I required full Boss resistance while retaining lower tunable resistance for normal enemy types.

## 5. Pre-Authored Procedural Room Generation
- **Prompt:** "hey now, you can remove the old map (the map before the generated dungeon). we don't need them anymore, and therefore, also remove this option in the dialouge... suggest how can i design where the objects will be located in each room, so that when generating room, we only need to pick some from the already designed rooms, without mock-up and randomly putting them across the room?"
- **Purpose:** Transition from completely random prop placement to pre-authored Tiled templates for procedural dungeon rooms.
- **Content Generated:** Created a plan (`plan_34_room_templates.md`) detailing the exact CSV layer structure and numbering IDs (e.g. 10 for Prop 1, 1 for Box, 12 for Mock Wall) for the user to design custom rooms. Cleaned up legacy map loading logic in `main.cpp` and removed outdated story prompts.

## 6. Animated 2.5D DoorGate
- **Prompt:** "Implement an animated `DoorGate` entity that transitions between a flat floor tile and a solid 2.5D wall to trap the player and enemies inside battle rooms... Frame 0 represents the door lowered (flat floor)... Frame 7 represents the door fully raised (tall wall)."
- **Purpose:** Add dynamic, animated door objects to procedurally generated rooms that logically block player escape during combat, utilizing the same 2-pass Y-sorting logic as tall props.
- **Content Generated:** Created `DoorGate` class with a 4-state machine (OPEN, CLOSING, LOCKED, OPENING). Hooked it up to `LevelManager` room transitions to trigger animations and toggle collision dynamically, replacing the previous static `doorColliders` rectangle arrays.

### **13. Main Menu Continue and Clean New Game**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 8, 2026.
* **Prompt used:**

```text
Fix returning from an active game to the main menu so a Continue button resumes
the current match. Make Start Game load a fully reset HUB session at the correct
player position and clear old projectiles and object entities.
```

* **Purpose of use:** To separate suspending an active session from starting a clean game and correct the HUB spawn position.
* **Content generated by AI:** Added a conditional Continue action that preserves and restores the active HUB or gameplay state, replaced direct menu state changes with explicit menu actions, and added a clean-session reset path for projectiles, effects, particles, dialogue, waves, level entities, and paladin state before reloading the HUB at its calculated center.
* **Independent work/validation:** I observed that returning to the menu destroyed the active match and that Start Game reused the player's previous position instead of creating a clean HUB session.

### **14. Admin Panel FPS Display**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 8, 2026.
* **Prompt used:** `Show the current FPS number in the admin panel.`
* **Purpose of use:** To make runtime performance visible while using developer controls.
* **Content generated by AI:** Added a live FPS counter to the admin panel header.
* **Independent work/validation:** I requested the FPS value specifically inside the existing admin panel.

### **15. Immediate Character-Swap Crash Fix**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 8, 2026.
* **Prompt used:** `Fix the crash when pressing Tab immediately after entering gameplay from the HUB.`
* **Purpose of use:** To prevent a newly selected paladin from updating through an invalid aiming-strategy pointer.
* **Content generated by AI:** Initialized all paladin aiming state and transferred the active aiming strategy during manual, numbered, and death-triggered character swaps before the new paladin updates.
* **Independent work/validation:** I reproduced the timing condition as an immediate Tab press after entering gameplay from the HUB.

### **16. Admin Enemy Cleanup Control**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 8, 2026.
* **Prompt used:** `Create a Delete All Enemies button in the admin panel.`
* **Purpose of use:** To let developers quickly clear spawned enemies during gameplay testing.
* **Content generated by AI:** Added an admin-panel action that queues every active enemy through the level manager's safe deferred-removal system and reports the number being removed.
* **Independent work/validation:** I requested a single cleanup control for removing all enemy types, including the Boss.

### **17. Enemy Pathfinding CPU Optimization Plan**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 8, 2026.
* **Prompt used:** `Create a plan to optimize the current enemy pathfinding CPU cost beyond changing numeric limits.`
* **Purpose of use:** To organize a reliable staged optimization of enemy navigation without restoring direct-player fallback movement.
* **Content generated by AI:** Created `plan_36_enemy_pathfinding_cpu_optimization.md`, prioritizing correction of the forced-per-frame scheduler, valid-path reuse, locked-room search bounds, bounded connection work, shared navigation caches, cheaper local avoidance, profiling, and an optional flow-field stage.
* **Independent work/validation:** I observed excessive CPU usage, experimented with longer update intervals and lower search limits, and requested optimizations that address the underlying workload rather than only changing constants.

### **18. Enemy Pathfinding CPU Optimization Implementation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** August 8, 2026.
* **Prompt used:** `Apply plan 16 in tmp for optimizing the enemy pathfinding CPU cost.`
* **Purpose of use:** To reduce repeated A* and collision-query work while preserving reliable footprint-aware navigation and no direct-player fallback.
* **Content generated by AI:** Replaced forced per-frame path searches with a bounded credit scheduler, reused valid paths until target or navigation changes, restricted procedural searches to locked-room bounds, bounded start recovery and waypoint smoothing, shared tile/edge collision caches by room revision and enemy footprint, reduced local-avoidance scans, and added optional pathfinding timing counters to the admin panel.
* **Independent work/validation:** I identified excessive CPU usage during enemy navigation and retained the tuned `0.6`-second interval, `70` search-step limit, and `7` target-position limit as the implementation baseline.
