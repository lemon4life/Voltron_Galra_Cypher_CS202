# AI Usage Notes - Week 8

### **1. Dynamic Camera & Procedural Melee System**

* **Tool:** Google Antigravity Agent, Gemini 2.0.
* **Access time:** July 28 - July 29, 2026.
* **Prompts used:**

```text
Please implement a dynamic, "Soul Knight" style camera system for our 2D top-down game.

the camera zooming too close, make them further

Please refactor Keith's attack animation. We are moving away from a fully frame-by-frame character animation to a procedural, inertia-based weapon swing paired with an animated sprite effect.

the Sword_slash_big.png is original a slash downward (from pointing up to downard clock wise). make sure Keith's attack should follow the slash direction, and the alternating attack (slicing upward), the slash should also be flipped. the two attack combo should be random.

Please implement Keith's melee attack system and weapon animation mechanics to match a "Soul Knight"-style sword swing.

go for the "The sprite only appear when the sword is at about +30 degree", the sprite will be triggering the effect on frame 0.
```

* **Purpose of use:** To implement an aim-biased, smooth-interpolating camera system that tracks between the player and the cursor, and to completely overhaul Keith's melee attack from static frame animations to a physics-based, procedural 120-degree arc swing with alternating slashes.
* **Content generated by AI:** A new `DynamicCamera` system with lerped tracking, procedural weapon swing logic using ease-in-out rotation, dynamic 120-degree swing arcs anchored at the weapon hilt, and randomized alternating attack direction logic perfectly synced with the 3-frame `Sword_slash_small` trail effect overlaid using additive blending.
* **Independent work/validation:** I specified the exact "Soul Knight" behavioral feel (e.g., cursor bias percentage, alternating slashes, and exact angular thresholds). I provided the 3-frame asset logic and directed the timing sync.

### **2. Centralized Constants and Uniform Scaling**

* **Tool:** Google Antigravity Agent, Gemini 2.0.
* **Access time:** July 29, 2026.
* **Prompts used:**

```text
Please create a dedicated configuration header file to centralize all global constants, and refactor the codebase to eliminate hardcoded values and individual scaling factors.

Please create a central configuration file for global constants and perform a project-wide refactor to fix scaling inconsistencies and map rendering artifacts. Strict Uniform Scaling Enforcement (NO Individual Scaling).

Diagnose and Fix Map/Tileset Glitching (Dark Lines & Dots). Texture Filtering / Bilinear Bleeding Fix, Floor / Round World Positions, Tile Inset (UV Padding / Bleed Correction).
```

* **Purpose of use:** To eliminate scattered magic numbers across `main.cpp` and multiple managers, strictly enforce a single uniform `GLOBAL_SCALE` variable for all game rendering, and eradicate floating-point precision artifacts causing dark grid lines in the tilemap.
* **Content generated by AI:** A newly authored `include/Core/Constants.h` file housing a `Constants` namespace for standard window dimensions (1280x720) and `GLOBAL_SCALE`. The AI swept `main.cpp`, `CameraManager`, and all attack scripts to strip local scalars, and applied `std::floor` wrapping, `TEXTURE_FILTER_POINT`, and a 0.05px UV inset to `LevelManager`'s tile rendering loop.
* **Independent work/validation:** I demanded a zero-exception rule to ban localized entity scaling variables. I also provided the specific technical solutions for fixing the tilemap glitches (UV padding, coordinate flooring) which the AI successfully translated into `LevelManager`.

### **3. Hitbox Architecture and Environment Physics**

* **Tool:** Google Antigravity Agent, Gemini 2.0.
* **Access time:** July 29, 2026.
* **Prompts used:**

```text
Please adjust the wall collision bounding boxes for all characters (player characters including Keith, and enemy types) to improve top-down movement feel.

Separate Hitbox Types: Ensure character physics/environment collisions are distinct from hurtboxes/hitboxes. Reduce Height: Reduce the height of the environment/wall collision box so it only covers the character's feet/base. Positioning: Anchor the bottom of the reduced collision box to the bottom edge of the character's sprite frame.
```

* **Purpose of use:** To solve 2.5D top-down perspective issues where characters' heads would get stuck on walls, by detaching the movement physics bounding box from the combat hurtbox.
* **Content generated by AI:** Added a new virtual `GetCollisionBox()` method to the `GameObject` base class, isolating environmental collision from the full-body `GetBoundingBox()` used in combat. Overrode `GetCollisionBox()` in `Paladin` and `Enemy` to return tightly-bound rectangles covering only the bottom 30% of the character sprite (their feet). Re-wired `EnemyCollision.cpp`, `EnemyPathManager`, and the Player state machine to test against this new box.
* **Independent work/validation:** I architected the split between hurtbox and physics collision box to preserve combat integrity while improving movement. I tested the game extensively to verify that environmental snags were fixed but sword slashes still registered correctly against the torso.

### **4. Enemy Sprite Polish and Effect Kinematics**

* **Tool:** Google Antigravity Agent, Gemini 2.0.
* **Access time:** July 29, 2026.
* **Prompts used:**

```text
double-check the scaling again in all files, espcially in sprites and enemies. I've update Lance_stab_small.png (4 frames), use it for the Enemies instead of the old Lance_stab.png.

double-check if the Lance_stab_small.png is used?

do not move the Lance_stab_small.png with the enemy's lunging.
```

* **Purpose of use:** To audit enemy sprite scaling under the new global system, switch the `EnemyDiver` to use the newly provided 4-frame lance stab asset, and fix a kinematic glitch where the attack visual was sliding awkwardly with the enemy's lunge momentum.
* **Content generated by AI:** The AI audited the codebase and applied the required `GLOBAL_SCALE` variable universally. It re-linked the `Lance_Stab` texture in `AssetManager` to `Lance_stab_small.png`. Additionally, it introduced `staticEffectPos` inside `EnemyDiver` to capture the world coordinates the exact frame an attack initiates, rendering the lance stab as a punchy, stationary "afterimage" flash instead of sliding.
* **Independent work/validation:** I created the new 4-frame asset myself and flagged the specific kinematic bug. After implementation, I verified through debugging that the effect correctly snapped to 4 frames (divided by 4.0f) natively and remained static on screen during dashes.


# AI Usage Notes - Week 8 - Phuc Khanh

### **1. Scalable Settings and Compact Pause Menu**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 29, 2026.
* **Prompt used:**

```text
Change the settings UI so that it scales with the game, add a Settings button
to the pause menu, and make the pause menu smaller in both dimensions.
```

* **Purpose of use:** To keep the settings popup aligned with the game's virtual resolution at every window size and make settings accessible without leaving a paused game.
* **Content generated by AI:** Moved settings drawing, hover detection, and button input into the 683x512 virtual UI coordinate system; added pause-to-settings navigation that preserves the frozen Hub or gameplay scene; added a Settings pause action and button; and reduced the pause container from 400x440 to 340x360 with compact controls.
* **Independent work/validation:** I required Settings Back and Escape to return to the pause menu when settings were opened from pause, while retaining the existing return to the main menu when settings were opened there. The unrelated existing deletion of `include/Entities/PathfindingEnemy.h` was preserved.

### **2. Gameplay, Dialogue, and Settings Rendering Comparison**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 31, 2026.
* **Prompt used:**

```text
Create a Markdown file in the tmp folder explaining whether normal gameplay,
dialogue, and Settings use different UI rendering paths.
```

* **Purpose of use:** To document the current camera, coordinate, layering, scaling, and background differences among the three UI rendering paths.
* **Content generated by AI:** Created a comparison of world-camera rendering, the 683x512 virtual UI camera, dialogue layering and internal scaling, Settings raw-window positioning, modal overlay behavior, and the resulting resize differences.
* **Independent work/validation:** I based the report on the current `src/main.cpp`, `DialogueManager`, and `UIManager` implementations and distinguished rendering behavior from update behavior. No runtime code was changed.

### **3. UI Rendering Optimization and Modal-Scale Refinement**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 31, 2026.
* **Prompt used:**

```text
Review the current project and recommend UI rendering optimizations for
gameplay, dialogue, Pause, and Settings. Make Settings scale like Pause, add a
Settings button to Pause, reduce the Pause dimensions, and clamp modal scaling
so buttons remain moderate. Put the result in tmp. Revise the recommendation so
the team HUD is not refined because it is outside the current scope.
```

* **Purpose of use:** To define a consistent rendering architecture and a concrete correction for oversized modal panels and mixed UI coordinate systems.
* **Content generated by AI:** Created a detailed recommendation document with separate gameplay and modal scale policies, a maximum modal scale of 1.35, a dedicated SettingsMenu design, audio-slider migration, a compact 320x256 Pause layout, Pause-to-Settings navigation, background preservation, dialogue scale simplification, affected files, implementation order, and acceptance checks. The revised scope explicitly leaves the team HUD unchanged.
* **Independent work/validation:** I based the dimensions and physical-size comparisons on the current 683x512 virtual resolution and existing UI component sizes. I retained uncapped viewport scaling for gameplay-aligned UI, excluded team-HUD refinement, and used an upper-only modal cap so small windows can still shrink menus enough to fit. No runtime code was modified.

### **4. Main Loop Summary**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 31, 2026.
* **Prompt used:**

```text
Provide a summarized explanation of main.cpp.
```

* **Purpose of use:** To document the responsibilities and control flow of the game's top-level entry point alongside the UI rendering recommendations.
* **Content generated by AI:** Created a Markdown summary covering initialization, deferred team setup, virtual coordinates, pause input, every GameState update path, Hub dialogue behavior, gameplay hitstop, rendering order, modal rendering, current UI limitations, recommended responsibility extraction, and cleanup.
* **Independent work/validation:** I summarized the current `src/main.cpp` implementation without changing runtime code and kept the team HUD outside the refinement scope.

### **5. UI Rendering and Modal Refinement Implementation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 31, 2026.
* **Prompt used:**

```text
Apply the ui_rendering_optimization_and_refinement.md plan, with the revised
scope that excludes all team HUD refinement.
```

* **Purpose of use:** To implement consistently scaled Settings and Pause screens without allowing modal buttons to grow excessively on large windows.
* **Content generated by AI:** Added a dedicated SettingsMenu with the existing audio controls, reduced Pause to a 320x256 four-button panel, added Pause-to-Settings navigation, introduced a centered modal camera capped at 1.35, mapped modal input through that camera, preserved gameplay behind Settings opened from Pause, and converted dialogue from redundant 1024-height scaling to direct 683x512 logical coordinates.
* **Independent work/validation:** I preserved the existing team HUD implementation and retained the full-window overlay outside the modal camera. The required CMake build is the validation gate for the implementation.

