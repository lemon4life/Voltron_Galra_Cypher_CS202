# **B. AI Usage Notes**

### **1. 4-Frame Animation & Sprite Coordinate Refactoring**

* **Tool:** AntiGravity (AI Coding Assistant).
* **Access time:** 11:30 on July 18, 2026.
* **Prompts used:** 
```text
the playable character idle and run sprite is still glitching, while the NPC (allura) is fine

the x positon for the weapon should be character.x + 12 pixel, the y position of weapon should be character.y + 22 pixel. when the character turn, or mouse turn, flip it.

nope sorry, the character.x and character.y i mention is the top-left of the character, when adding 12 and 22, the weapon should be at somehow center of the character. can you fix it?
```
* **Purpose of use:** To debug glitchy animation looping across state machines and implement precise, pixel-perfect weapon origin math based on top-left offsets for dynamic rendering and flipping.
* **Content generated by AI:** Code that locked the animation loop bounds to 4 frames (preventing division rendering errors) inside the `PlayerState` machine, as well as calculated mathematical offsets (`position.x +/- 4.0f` and `position.y + 6.0f`) to properly map top-left visual coordinates to the engine's center-based bounds logic.
* **Independent work/validation:** I instructed the AI specifically on the desired target coordinates (+12x, +22y relative to top left), and validated that the resulting output properly mapped those offset numbers onto the engine's center-based drawing logic without hard-coding magical numbers incorrectly. When the AI first tried a direct addition that broke alignment, I corrected it to explicitly calculate from the top-left corner origin.

### **2. Punchy Gun Shooting System (Lance)**

* **Tool:** AntiGravity (AI Coding Assistant).
* **Access time:** 15:44 on July 18, 2026.
* **Prompts used:** 
```text
read all the new files in our project, I have updated the characte sprites into 32x32, 4 frames, beforehand. Now, Please implement a "punchy" gun shooting system for Lance. I have added new visual assets to the `assets/sprites/Lance/` directory. Please integrate the following mechanics:
1. Recoil Effect...
2. Muzzle Flash...
3. Bullet Trajectory...
4. Hit Impact Animation...

the muzzle flash should also appear when the bullet hit the wall (or any collision in the future). can the weapon be rotated but without having the pixel mess up?
```
* **Purpose of use:** To implement an action-oriented shooting mechanic for the character Lance, complete with visual recoil interpolation, dynamic muzzle flashes using trigonometry, rotated bullet sprites, and animated impact effects upon enemy/wall collision.
* **Content generated by AI:** Extended the `GameManager` to track and render `ImpactEffect` structs; refactored `RangedAttackStrategy` to apply and lerp a `recoilOffset` vector and calculate barrel tip coordinates for `Muzzle_Flash.png`; overloaded `Projectile` to support directional `TEXTURE_FILTER_POINT` textures instead of raw geometry.
* **Independent work/validation:** The user provided all visual assets upfront and defined the exact mechanical feel (recoil snap, trigger timing, hit impacts). The user also identified a visual bug with texture filtering on rotation and instructed the AI to correct it.

### **3. Melee Combo System & Dynamic Hitboxes (Keith)**

* **Tool:** AntiGravity (AI Coding Assistant).
* **Access time:** 18:49 on July 18, 2026.
* **Prompts used:** 
```text
Please implement the melee sword attack animation and hitbox systems for Keith. 
Use the following assets and technical specs:
- Attack Sequence 1: "assets/sprites/Keith/Attack_1.png" (4 horizontal frames, grid sheet)
- Attack Sequence 2: "assets/sprites/Keith/Attack_2.png" (4 horizontal frames, grid sheet)

Please implement the following technical architecture:
1. Combo State Machine...
2. Frame-by-Frame Animation Layout...
3. Melee Sword Hitbox Logic...
```
* **Purpose of use:** To overhaul the `MeleeAttackStrategy` into a dynamic state machine capable of input buffering to chain a 2-hit melee combo, slicing multi-frame sprite sheets, and creating precise, frame-specific spatial hitboxes to apply damage and knockback only on impact frames.
* **Content generated by AI:** Wrote an implementation plan to update `MeleeAttackStrategy.h/.cpp` with variables for combo steps, frame tracking, and input buffering. Devised a method to spawn dynamic AABB hitboxes in `Update()` dependent on `currentFrame == 1 || currentFrame == 2`.
* **Independent work/validation:** The user strictly mapped out the technical requirements (buffer windows, specific impact frames, exact assets to load) and logic flow for chaining animations rather than returning to idle.


## Refined Dash Animation
**Prompt:** `read all the new files of this project, I have added two new 1-frame sprites to the Lance/ directory: Dash_back.png and Dash_front.png. Please update the character movement and animation state machine to handle these... Please refine the dash movement logic we just implemented with the following adjustments: Ensure there is no grayscale or color tint applied to the character during the dash state... Replace the linear movement during the dash with a smooth, inertia-based gliding motion...`
- **Purpose:** Implement directional dash animations and smooth sine-wave easing motion physics.
- **Generated Content:** Modified `PlayerDashState::Update` to calculate inertia-based physics, removed grayscale tint in `Paladin::Draw`, and loaded new `Dash_front`/`Dash_back` textures into `CharacterSprites`.


## Upscaled Rotatable Texture Pipeline
**Prompt:** `Discard any previous instructions about using nearest-neighbor filtering... Treat all rotatable .png assets like standard, high-resolution pictures... I want to fix the "muggy" and distorted look of rotated pixel art by upscaling the assets into high-resolution images in the code before rendering them... Upscale that Image by a significant factor (e.g., 4x or 8x) using Raylib's ImageResizeNN()...`
- **Purpose:** Fix pixel distortion during rotation by pre-upscaling the raw sprites and applying bilinear hardware filtering.
- **Generated Content:** Created `LoadUpscaledTexture` helper in `main.cpp` to natively upscale rotatable sprites 4x in RAM. Modified rendering math (`barrelLength`, `dest`, `origin`) in `LaserAttackStrategy`, `MeleeAttackStrategy`, and `RangedAttackStrategy` to inversely scale back down by 4.0f to preserve physics and layout.


## Rotated Fat Pixel Rendering Pipeline
**Prompt:** `Discard any previous instructions about using nearest-neighbor filtering... I have a reference image from Soul Knight that shows exactly how I want the rotation to look. The "pixels" on the rotated gun are actually large, sharp squares that rotate smoothly in high-resolution space without stair-stepping or blurring... Do NOT render the game to a low-resolution RenderTexture2D and scale it up... set the Camera2D zoom to a high value...`
- **Purpose:** Implement a native high-resolution camera scaling engine to preserve fat chunky pixels that rotate natively without rigid grid stair-stepping or blurry sub-pixel interpolation.
- **Generated Content:** Removed `RenderTexture2D target` loop in `main.cpp`. Configured `camera.zoom = 2.0f` and `uiCamera.zoom = 2.0f`. Switched `GetMousePosition` to use native `GetScreenToWorld2D` translation. Restored all `TEXTURE_FILTER_POINT` logic on native sprites, while successfully removing all `4.0f` upscaling math from the combat strategy scripts.


## Keith Melee Attack Randomization
**Prompt:** `that's nice, but there is a little bug with the sprite of Keith's attack isn't rendered properly, instead of alternating, make them random, can be 1 or 2 when the player attack`
- **Purpose:** Make Keith's combo attack sprites trigger randomly instead of strictly alternating between 1 and 2.
- **Generated Content:** Modified `MeleeAttackStrategy::Attack` and `Update` to use `(rand() % 2) + 1` instead of alternating the `comboStep` state.


## Parry and Hitstop Mechanic
**Prompt:** `I have added a 1-frame Parry.png sprite to Lance/. Please implement a Parry mechanic and hitstop effect with the following requirements: Add a PARRY state. When the player presses 'F' to parry an attack, lock their movement and set their state to PARRY for exactly 0.2 seconds. During this state, render the Parry sprite...`
- **Purpose:** Introduce a skill-based parry mechanic with visual hitstop effects and orthogonal weapon blocking.
- **Generated Content:** Modified GameManager to support logic bypassing for `hitstopTimer`. Added `PlayerParryState` for 0.2s block window on key `F`. Modified `EnemyChaseState` to trigger Hitstop, calculate orthogonal parry angle, and knockback the enemy instead of dealing damage if colliding during the parry window.


## Parry Stance Mechanics Refinement
**Prompt:** `Let's refine the Parry state mechanics and fix a sprite rotation bug. Please implement the following rules for when the player holds 'F' (Parry) and has not taken damage yet: State & Input Freeze... Weapon 90-Degree Block Angle... Bug Fix - Character Rotation...`
- **Purpose:** Refine the Parry stance to be a held action that locks movement and aiming, while correctly displaying the `parry.png` texture as a character state instead of a rotated weapon sprite.
- **Generated Content:** Updated `PlayerParryState` to last as long as `KEY_F` is held. Updated `Paladin` logic to lock `aimTarget` and `facingLeft` while parrying. Fixed `Paladin::Draw()` so `parry.png` acts as the character texture (flipping horizontally only) while the weapon is drawn at a locked 90-degree block angle.


## Player Knockback on Parry
**Prompt:** `Let's implement the core mechanics for a successful Parry. When the player is in the Parry state (pressing 'F') and successfully blocks an incoming hit, I want the character to be physically pushed back slightly...`
- **Purpose:** Give physical weight to parrying by pushing the player back using smooth velocity decay when they block an attack.
- **Generated Content:** Added `knockbackVelocity` to `Paladin` class. Implemented smooth lerp-style decay on knockback velocity applied directly to the character's movement in `Paladin::Update`. Called `ApplyKnockback` during `TriggerParrySuccess` in the opposite direction of the attacker.


## Damage Immunity and Parry Limit
**Prompt:** `Let's expand on the Parry mechanics. In addition to the directional knockback on a successful block we discussed, please implement a damage mitigation and parry limit system...`
- **Purpose:** Balance the parry mechanic by enforcing damage immunity strictly on success, and limiting consecutive parries to 3 before punishing the player.
- **Generated Content:** Added `consecutiveParries` counter to the Paladin. Updated `EnemyChaseState` to enforce a max limit of 3 parries, beyond which the player takes damage normally and their parry stance is broken. Implemented reset conditions in `PlayerRunState` and `PlayerAttackState`. Added visual spark effect on block.


## Parry Bug Fix: Projectiles and Directional Checks
**Prompt:** `I've encountered a critical bug with the Parry system we just implemented. The Bug: When I press and hold 'F' to parry, the character still takes full damage from enemy attacks...`
- **Purpose:** Fix a bug where enemy projectiles completely bypassed the parry state, and implement a skill-based directional blocking cone.
- **Generated Content:** Updated `GameManager::UpdateProjectiles` to intercept projectile collision with a parry check, triggering success and destroying the projectile. Added `Paladin::CanParryAttack(Vector2)` to enforce a 180-degree frontal cone directional check using dot product math—meaning you must face the attacker to successfully parry!


## Parry Bug Fix: EnemyChaserState override
**Prompt:** `the bug is still there.`
- **Purpose:** Fix a bug where physical enemies were still bypassing the parry check.
- **Generated Content:** Discovered that `EnemyChaserState.cpp` and `BossState.cpp` were completely overriding the base `EnemyState.cpp` collision logic. Patched both files to correctly integrate the `activePaladin->CanParryAttack(ePos)` checks into their respective `CheckCollisionRecs` logic blocks so all enemies respect the parry.


## Hitstop Camera Polish & Parry Break Fix
**Prompt:** `Let's polish the hitstop effect and fix two collision/state bugs...`
- **Purpose:** Implement smooth camera panning and zooming during hitstop (which freezes entities but not the camera). Fix the parry persistence bug so the stack break snaps out of the stance immediately. Fix the knockback logic so it doesn't push enemies through walls.
- **Generated Content:** Modified `main.cpp` to decouple camera update logic from the Hitstop timer and implemented `Lerp` logic for fluid zooming and panning. Adjusted `Paladin.h`'s `IncrementParryCount` to instantly snap the player to `IdleState` upon reaching 3 parries. Updated `EnemyState`, `EnemyChaserState`, and `BossState` to properly integrate environment AABB checks during the heavy 60px parry pushback to prevent out-of-bounds wall clipping.


## AssetManager Integration & Fat Pixel Rendering Restore
**Prompt:** `read all the new files: we have add window resizable feature, Pathfinding Enemy hierachy and reorganize enemy files, and change the Observer type to a better design pattern. However, the fat pixel Rotated Pixel we implemented before met a trouble and lost, please try to do it again. And I want to keep the main.cpp organize, now they have too many LoadTexture and State, can we seperate them into new files for easier access and organize, try implement some kind of design pattern.`
- **Purpose:** Implement an `AssetManager` Singleton to handle loading and distributing textures. Refactor `main.cpp` to remove manual texture loading. Restore the high-resolution 'Fat Pixel' rotation that was broken when a `RenderTexture2D` scaling pipeline was introduced.
- **Generated Content:** Created `AssetManager.h` and `AssetManager.cpp` providing centralized texture caching and sprite struct generation (`LoadCharacterAssets`, `GetLanceSprites`, etc.). Completely rewrote `main.cpp`'s rendering loop: removed `RenderTexture2D` and implemented dynamic `Camera2D` zooming based on the current resizable window's dimensions. Re-applied `TEXTURE_FILTER_POINT` to rotatable assets within `AssetManager` so they correctly rotate at the native window resolution without distortion.


## Attack Render and Parry Positioning Fixes
**Prompt:** `Fix these bug: The attack render of Keith look off... The attack of Hunk should stay in the last position... In parry state, the weapon should also be slightly in front of the character...`
- **Purpose:** Fix visual bugs involving Keith's attack swing flipping awkwardly, Hunk's laser tracking the mouse continuously during the firing animation, and making the Parry shield visually offset from the center while tracking attackers.
- **Generated Content:** Modified `MeleeAttackStrategy.cpp` to use vertical mirroring instead of horizontal to fix Keith's backwards swing when facing left. Refactored `LaserAttackStrategy.cpp` to decouple the laser tracking angle from the player's active aim, calculating the draw angle directly against the fixed `laserEndPoint` to simulate fixed beam projection. Updated `Paladin.cpp` to ignore mouse input during parry, snap the `aimTarget` directly to the attacker upon a successful block, and offset the shield asset 12 pixels towards the attack vector.


## UI Visual Polish (Run Dust & Player Circle)
**Prompt:** `I have update the Run sheet for all character to 8 frames instead of 4 frames. In assets/UI/, there are Player_Circle.png (one frame) and Run_Dust.png (4 frames)...`
- **Purpose:** Implement Soul Knight-style character shadow circle and running dust trails, and update the character run animation configuration.
- **Generated Content:** Updated `PlayerRunState::Enter` to set `player->SetNumFrames(8)`. Refactored `GameManager`'s hardcoded impact effects system into a generic `AddEffect` system capable of playing arbitrary animated textures. Integrated the `Run_Dust.png` effect into the `Paladin::UpdateFootsteps` timer so it naturally spawns behind the character as they run. Added the `Player_Circle.png` rendering step underneath the character sprite in `Paladin::Draw`.
