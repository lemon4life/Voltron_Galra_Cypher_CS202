# AI Usage Notes - Week 7 - Phuc Khanh

### **1. Enemy Navigation Access and Collision Refactor Planning**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompts used:**

```text
Create a precise plan to expand IEnemyPathAccess with blocked-state, local-direction, and other level-backed methods currently reached through GameManager, and universalize repeated enemy collision checks with function prototypes.

Revise the plan so Boss inherits PathfindingEnemy instead of moving the path-access dependency into Enemy, allowing Enemy::UpdateKnockback() to reach it through a virtual accessor.

Rewrite Plan 2 so knockback implementation moves into PathfindingEnemy, Boss inherits it and actively uses pathfinding to chase, the naive EnemyChaseState is removed, Chase distances live in enemy entities, and shared collision/parry checks preserve all existing state logic.

Update Plan 2 so every access service supplied by LevelManager uses a required non-owning reference instead of a nullable pointer.

Revise Plan 2 so Enemy and PathfindingEnemy are merged into one Enemy class, remove the intermediate PathfindingEnemy type, and update all path interfaces and manager relationships to operate on Enemy directly.
```

* **Purpose of use:** To remove enemy and enemy-state dependence on `GameManager::GetLevelManager()`, restrict navigation through the injected level-access interface, and consolidate repeated wall, player, enemy, and parry collision checks.
* **Content generated by AI:** A rewritten Plan 2 with a single `Enemy` base that owns required removal/path references, per-agent waypoint state, path registration, and knockback; `IEnemyPathAccess` and `EnemyPathManager` prototypes that operate directly on `Enemy`; deletion of `PathfindingEnemy`; Boss pathfinding Chase; reference-based factory wiring; shared collision checks; entity-owned disengagement queries; and verification steps.
* **Independent work/validation:** I requested planning only and required all existing attack, cooldown, Range, Diver, Boss-skill, and parry behavior to remain unchanged. I chose to remove the intermediate pathfinding base while keeping path algorithms in `EnemyPathManager`, and the plan preserves the exact parry condition and consequences in their current order. No runtime code was modified.

### **2. Enemy and PathfindingEnemy Merge Implementation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompts used:**

```text
Apply Plan 2 into the project so Enemy is merged with PathfindingEnemy.
```

* **Purpose of use:** To simplify the enemy hierarchy, remove singleton-based level access from enemy movement, make Boss use shared pathfinding, and consolidate repeated collision movement without changing established combat behavior.
* **Content generated by AI:** Merged path registration, waypoint storage, path-access references, and knockback into `Enemy`; removed `PathfindingEnemy`; changed all concrete enemies and `EnemyPathManager` to use `Enemy`; converted LevelManager access services to required references; added shared enemy collision and wall-movement helpers; migrated Chaser, Range, Diver, and Boss states; and removed the unused generic Chase implementation.
* **Independent work/validation:** I required enemy values, ranged prediction, Diver attack timing, Boss burst behavior, and Chaser parry consequences to remain unchanged. I reviewed the resulting dependency boundaries and validated the refactor with repository searches and the required CMake build.

### **3. Chase-State Path Registration Lifecycle**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompts used:**

```text
Move StartPathFinding() for Chaser, Diver, and Range into ChaseState::Enter() so enemies are not repeatedly added to PathManager during Update().

Remove Chaser's direct close-range movement so it always uses the same path-target flow as the other enemies.
```

* **Purpose of use:** To make path-manager registration match the Chase-state lifecycle and eliminate redundant per-frame registration calls.
* **Content generated by AI:** Chaser, Diver, and Range now register once when entering Chase and unregister through Chase `Exit()`. Redundant `StartPathFinding()` and transition-time `EndPathFinding()` calls were removed from Chase updates, and Chaser now always obtains its movement target through `IEnemyPathAccess`.
* **Independent work/validation:** I kept pathfinding active while an enemy remains in Chase, including when Diver pauses inside its clear dive range. I checked registration and Chaser target-selection call locations. The affected sources compiled, but final executable linking remained blocked by linker exit status 116.

### **4. Diver Lunging Parry**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompt used:**

```text
Add parry handling to the Diver LungingState.
```

* **Purpose of use:** To let the active Paladin block a Diver during its damaging lunge using the existing directional and swap-window parry rules.
* **Content generated by AI:** The Lunging contact branch now uses the shared parry query, triggers existing parry feedback and counting, prevents player damage, applies wall-safe pushback to the Diver, and immediately starts dive recovery. Non-parried contact retains the existing one-hit damage behavior.
* **Independent work/validation:** I reused the current `CanParryAttack()` rules and Chaser feedback values instead of creating a separate Diver parry system. The one-contact flag is set before either outcome so a parry or hit cannot trigger repeatedly during the same lunge.

### **5. Shared Enemy Daze State**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompt used:**

```text
Give every enemy a daze duration and an owned DazeState with a dTimer that returns to Chase only when the timer reaches zero. Apply it to Chaser and Diver parries instead of using attack cooldown as a stun.
```

* **Purpose of use:** To separate temporary stun behavior from attack cooldown and make daze timing available consistently to every enemy type.
* **Content generated by AI:** Added a reusable `EnemyDazeState`, a separate state instance for every Enemy, configurable daze duration and remaining-time accessors, and timer-based return to Chase. Successful Chaser and Diver parries now transition into Daze after their existing feedback and wall-safe pushback.
* **Independent work/validation:** I kept attack cooldown separate from the daze timer. Diver resets its normal dive cooldown before entering Daze, while Chaser no longer uses a hard-coded attack cooldown value as a stun timer.

### **6. Chaser Damage-State Planning**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompt used:**

```text
Create a small plan that moves Chaser player damage into a separate Damage state, stops pathfinding movement within 5 units, and performs a short charge of at most 10 units before returning immediately to Chase.
```

* **Purpose of use:** To separate Chaser navigation from its attack resolution and define a short, readable attack phase without adding recovery time.
* **Content generated by AI:** A focused implementation plan with entity-owned stop/charge constants, an owned typed Damage state, fixed-direction charge timing, wall and player collision handling, cooldown resolution, parry-to-Daze behavior, immediate Chase return, and verification cases.
* **Independent work/validation:** I specified that Chase retains cooldown and path movement but no longer owns player damage or parry effects. The plan consumes missed or wall-blocked attempts by resetting cooldown to prevent immediate Damage/Chase state loops.

### **7. Chaser Damage-State Implementation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompt used:**

```text
Apply the Chaser Damage-state plan.
```

* **Purpose of use:** To implement the planned separation between Chaser navigation and contact attacks.
* **Content generated by AI:** Added entity-owned stop, charge-distance, and charge-duration configuration; an owned typed `EnemyChaserDamageState`; fixed-direction wall-aware charge movement; one-contact damage and parry resolution; cooldown consumption for hits and failed attempts; and immediate transitions to Chase or Daze.
* **Independent work/validation:** I retained the existing Chaser health, speed, damage, sight, cooldown, pathfinding, parry feedback, and Daze behavior. Chase now decides when to attack but contains no player collision or damage logic, and Damage cannot travel more than 10 units.

### **8. Chaser Charge Range and Pass-Through**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompt used:**

```text
Increase the Chaser charge range so it quickly passes through the player and damages the player if contact occurs.
```

* **Purpose of use:** To make the Damage-state charge visibly fast and long enough to pass the target while keeping contact detection reliable.
* **Content generated by AI:** Increased the charge to 200 units over 0.2 seconds, added body-sized movement substeps to prevent high-speed collision tunneling, and changed a normal hit to continue the remaining charge after dealing damage once. Wall collision and successful parry still interrupt the charge immediately.
* **Independent work/validation:** I preserved fixed-direction movement, the one-hit flag, attack cooldown resolution, parry feedback, Daze transition, and immediate return to Chase when the charge finishes.

### **9. Chaser Short Melee-Dash Tuning**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompt used:**

```text
Retune the Chaser charge into a short, moderate-speed melee dash that travels only far enough to pass through an idle player.
```

* **Purpose of use:** To replace an excessively long and fast charge with a compact Soul Knight-style melee movement.
* **Content generated by AI:** Tuned the charge to 64 units over 0.16 seconds, producing a 400-unit-per-second fixed dash. The value accounts for the Chaser's 20x20 body, the player's 16x24 body, and the 30-unit attack-entry distance while retaining swept one-hit collision detection.
* **Independent work/validation:** I kept the existing wall interruption, parry interruption, pass-through damage, cooldown resolution, and immediate return to Chase. Only charge distance and duration were retuned.

### **10. Grid-Backed Destructible Box Planning**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 22, 2026.
* **Prompt used:**

```text
Create a new destructible-box plan that places boxes in the first map layer, keeps their cells solid while they exist, lets swords and bullets destroy them, updates the map grid through a destruction service, and replaces GameObject dynamic_cast checks with an enum type.
```

* **Purpose of use:** To design a destructible cover object whose health is represented by a logical entity while collision remains controlled by the map grid.
* **Content generated by AI:** A new numbered implementation plan defining a dedicated Layer-1 box marker, `DestructibleBox`, an immutable `GameObjectType`, a narrow grid-object destruction service, safe deferred grid replacement and deletion, sword and bullet collision ordering, object-tag migration, edge cases, and verification steps.
* **Independent work/validation:** I required the plan to keep Layer 1 as the collision source while a box is intact and to restore the cell to floor only through LevelManager. I also limited enum-based cast replacement to casts originating from `GameObject*` and retained unrelated strategy/state casts. No runtime code was modified.

### **11. Separate Game-Object Map Layer Planning**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 24, 2026.
* **Prompts used:**

```text
Create a separate layer for game objects that is independent from the first two map-building layers.

Add an explicit IsSolidMapObject() if branch for DestructibleBox with a short comment, amend Plan 5, and apply the complete plan to the project.
```

* **Purpose of use:** To separate runtime object placement and lifetime from graphical map construction, then implement a destructible box whose collision is controlled by that object layer.
* **Content generated by AI:** Amended Plan 5 and implemented a non-rendered `Game Objects.csv`, semantic object IDs, validated object-grid loading, five box placements, `DestructibleBox`, an explicit `IsSolidMapObject()` branch, grid-backed collision, deferred destruction-service clearing, sword and bullet damage, immutable `GameObjectType` tags, and removal of casts originating from `GameObject*`.
* **Independent work/validation:** I required Layers 1 and 2 to remain unchanged when a box is destroyed and used the separate object cell as the only authority for physical box collision. I kept unrelated Boss-state and weapon-strategy casts outside this refactor and checked that all five box cells align with open map tiles.

### **12. Destructible-Box Code Update Documentation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 24, 2026.
* **Prompt used:**

```text
The current code diff cannot be opened, so create a Markdown file that states all of the code updates.
```

* **Purpose of use:** To provide a readable replacement for the raw Git diff of the game-object layer and destructible-box implementation.
* **Content generated by AI:** A comprehensive Markdown report covering layer responsibilities, semantic object IDs, map placement, loading, collision, destruction services, box behavior, sword and bullet integration, object-type migration, file-by-file changes, runtime flow, validation, manual tests, and known limitations.
* **Independent work/validation:** I based the report on the current working tree, verified all three map grids are 20x20, listed the five actual box cells, separated the feature changes from the unrelated `AGENTS.md` modification, and recorded the exact linker failure rather than claiming successful runtime validation.

### **13. Melee Enemy Knockback Correction**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 24, 2026.
* **Prompt used:**

```text
Fix the knockback in MeleeAttackStrategy where enemy position is changed directly instead of using the existing knockback function.
```

* **Purpose of use:** To route melee knockback through the shared enemy knockback system so movement decays normally and respects wall collision.
* **Content generated by AI:** Replaced the direct 20-pixel enemy position offset with `Enemy::ApplyKnockback()` and introduced a named `MELEE_KNOCKBACK_FORCE` constant of `350.0f`.
* **Independent work/validation:** I selected the existing projectile-strength impulse because, with the enemy's current knockback decay, its total movement is close to the previous 20-pixel displacement while using the shared wall-aware movement path.

### **14. Chaser Aggro and Ranger Aim Variation**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 25, 2026.
* **Prompts used:**

```text
Add a Chaser aggro meter that builds while the player is within 30 units, requires a random 1-2 seconds before attacking, and decays at half real time outside that range. Make each Ranger shot randomly choose predictive or direct player aim and add a small angular error.

Remove the visible Chaser aggro UI, change aggro detection to 40 units, preserve aggro outside the range, and randomize the required buildup between 0.2 and 0.7 seconds.

Keep the Chaser attack distance unchanged but make its charge movement slower.
```

* **Purpose of use:** To make melee attacks more readable and ranged attacks less mechanically perfect.
* **Content generated by AI:** Added per-Chaser randomized aggro thresholds, 40-unit proximity accumulation, stored out-of-range aggro, and attack consumption without an on-screen meter. Slowed the 64-unit charge by increasing its duration from 0.16 to 0.25 seconds. Ranger shooting randomly selects predictive interception or the player's current position and applies up to three degrees of random angular spread.
* **Independent work/validation:** I kept the 40-unit aggro range separate from the existing 30-unit attack distance, required both attack cooldown and aggro readiness, preserved accumulated aggro outside the detection range, and rerolled a 0.2-0.7 second threshold after every initiated attack. The charge keeps its 64-unit reach while its speed decreases from 400 to 256 units per second. Ranger velocity estimation continues every frame even when direct-shot mode is selected.

### **15. Main Menu and Pause GUI**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 25, 2026.
* **Prompts used:**

```text
Create a GUI implementation plan with a new GUI folder, a main menu containing two buttons, and a pause screen containing Quit Game, Back to Main Menu, sound-effect volume, and music volume controls.

Update the GUI plan with a Resume button and place all pause text, buttons, and sliders inside a container drawn above the darkened gameplay layer.

Apply the GUI plan directly to the project.

Move all GUI components into the existing UI module and update the related
code.
```

* **Purpose of use:** To replace inline keyboard-only menu code with reusable, scalable GUI screens and add runtime audio controls.
* **Content generated by AI:** Created reusable button and slider components, a two-button `MainMenu`, and a container-based `PauseMenu` with Resume, Back to Main Menu, Quit Game, sound-effect volume, and music volume controls. Consolidated those components under `include/UI` and `src/UI`, updated their include paths, added persistent AudioManager volume APIs, virtual-resolution mouse handling, safe menu return and quit actions, and guarded the old HUD pause button from activating through the pause overlay.
* **Independent work/validation:** I selected Play and Quit Game as the two initial main-menu buttons, retained keyboard pause/resume and the hidden demo shortcut, kept every pause control inside the centered container, and kept GUI components independent from direct global state mutation. I also required newly loaded audio to inherit the selected volume and reviewed the main-loop transitions before building.

### **16. Hub Pause and Layered Rendering Plan**

* **Tool:** Codex, GPT-5, OpenAI coding assistant.
* **Access time:** July 25, 2026.
* **Prompt used:**

```text
Create a plan that adds pause to the Hub, saves the previous GameState in
GameManager, resumes to that previous state, renders the game normally without
special pause rendering, and draws the pause layer above everything.

Apply Plan 7 directly to the project.
```

* **Purpose of use:** To make pause work consistently in both the Hub and gameplay while simplifying the distinction between the frozen state and the state used for rendering.
* **Content generated by AI:** Created and applied `GameManager` pause/resume and render-state APIs, centralized keyboard and HUD pause input, made HUD drawing side-effect free, transferred Escape-key ownership from Raylib to the game, separated update and render states, added a final pause-overlay pass, and migrated effect updates out of rendering.
* **Independent work/validation:** I selected a single suspended state instead of a general state stack because only one modal pause layer is currently required. I required Hub dialogue and gameplay to remain frozen behind the overlay, Resume to restore the exact source state, the pause container to render after all world and HUD content, and player aiming and effect timers to stop updating while paused.
